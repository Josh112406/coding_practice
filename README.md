# Python Coding Practice

## Set 1 — Custom String Methods (Right / End)

Reimplementations of built-in Python string methods without using the original function.

| # | File | Description |
|---|------|-------------|
| 1 | `1/rstrip.py` | Remove space characters at the end of a string (without `rstrip()`) |
| 2 | `1/removesuffix.py` | Remove matching characters at the end of a string (without `removesuffix()`) |
| 3 | `1/upper.py` | Convert all characters to upper case (without `upper()`) |
| 4 | `1/islower.py` | Check if all characters are lower case (without `islower()`) |
| 5 | `1/startswith.py` | Check if a string begins with a given substring (without `startswith()`) |
| 6 | `1/rjust.py` | Pad the beginning of a string with spaces to a given width (without `rjust()`) |
| 7 | `1/zfill.py` | Pad the beginning of a string with zeros to a given width (without `zfill()`) |
| 8 | `1/count.py` | Count how many times a substring appears in a string (without `count()`) |
| 9 | `1/index.py` | Return the first position of a substring in a string (without `index()`) |
| 10 | `1/rindex.py` | Return the last position of a substring in a string (without `rindex()`) |

## Set 2 — Basic Operations, Conditionals & Loops

Programs that practice input handling, arithmetic, comparisons, and loop constructs.

| # | File | Description |
|---|------|-------------|
| 1 | `2/print_smaller_number.py` | Input 2 numbers, print the smaller one |
| 2 | `2/print_not_equal.py` | Input 2 numbers, print "Not Equal" when they differ |
| 3 | `2/print_difference.py` | Input 2 numbers, print their difference |
| 4 | `2/integer_division.py` | Input 2 numbers, print the quotient without decimals |
| 5 | `2/print_remainder.py` | Input 2 numbers, print the remainder of division |
| 6 | `2/subtract_ten_numbers.py` | Input 10 numbers, print the first minus all the rest |
| 7 | `2/count_even_numbers.py` | Input 10 numbers, count how many are even |
| 8 | `2/odd_numbers_while_loop.py` | Print all odd numbers from 0 to 100 using a while loop |
| 9 | `2/exclude_zero_and_five_endings.py` | Print 0–100 excluding numbers ending in 0 or 5 |
| 10 | `2/print_numbers_between.py` | Input 2 numbers, print all numbers between them |

## Set 3 — Basic Operations, Conditionals & Loops (Part 2)

Programs that practice input handling, arithmetic, comparisons, and loop constructs.

| # | File | Description |
|---|------|-------------|
| 1 | `3/print_bigger_number.py` | Input 2 numbers, print the bigger one |
| 2 | `3/print_equal.py` | Input 2 numbers, print "Equal" when they are the same |
| 3 | `3/print_sum.py` | Input 2 numbers, print their sum |
| 4 | `3/print_product.py` | Input 2 numbers, print their product |
| 5 | `3/decimal_division.py` | Input 2 numbers, print the quotient with decimals |
| 6 | `3/power_of_numbers.py` | Input 2 numbers, print the first raised to the second |
| 7 | `3/sum_ten_numbers.py` | Input 10 numbers, print the sum of all |
| 8 | `3/count_odd_numbers.py` | Input 10 numbers, count how many are odd |
| 9 | `3/even_numbers_for_loop.py` | Print all even numbers from 0 to 100 using a for loop |
| 10 | `3/exclude_zero_endings.py` | Print 0–100 excluding numbers ending in 0 |

## Set 4 — Lists: Duplicates, Sorting & Aggregation

Programs that practice list operations with continuous input until invalid.

| # | File | Description |
|---|------|-------------|
| 1 | `4/find_duplicates.py` | Input 10 numbers, display all numbers that have duplicates |
| 2 | `4/most_duplicated_number.py` | Continuous input, display the number with the most duplicates |
| 3 | `4/find_highest_number.py` | Continuous input, display the highest number |
| 4 | `4/sort_highest_to_lowest.py` | Continuous input, display numbers sorted highest to lowest |
| 5 | `4/calculate_average.py` | Continuous input, display the average |

## Set 5 — String Formatting & Manipulation

Programs that practice string transformations and formatting.

| # | File | Description |
|---|------|-------------|
| 1 | `5/strip_leading_spaces.py` | Remove leading spaces from user input |
| 2 | `5/zero_padded_number.py` | Display a number in 6-digit zero-padded format |
| 3 | `5/to_uppercase.py` | Convert input to all capital letters |
| 4 | `5/to_lowercase.py` | Convert input to all lower case |
| 5 | `5/proper_casing.py` | Convert incorrectly cased input to proper casing |
| 6 | `5/reverse_casing.py` | Reverse the case of each character in the input |
| 7 | `5/count_words.py` | Count the number of words in a statement |
| 8 | `5/count_characters.py` | Count the number of characters in the input |
| 9 | `5/to_pascal_case.py` | Convert incorrectly cased input to PascalCase |
| 10 | `5/to_snake_case.py` | Convert incorrectly cased input to snake_case |

## Set 6 — Lists: Uniqueness, Sorting & Aggregation (Part 2)

Programs that practice list operations with continuous input until invalid.

| # | File | Description |
|---|------|-------------|
| 1 | `6/find_unique_numbers.py` | Input 10 numbers, display all numbers without duplicates |
| 2 | `6/remove_duplicate_entries.py` | Input 10 numbers, display all but only the first occurrence of duplicates |
| 3 | `6/check_unique_or_duplicate.py` | Continuous input, display "Unique" or "Duplicate" after each entry |
| 4 | `6/find_lowest_number.py` | Continuous input, display the lowest number |
| 5 | `6/sort_lowest_to_highest.py` | Continuous input, display numbers sorted lowest to highest |

## Set 7 — Custom String Methods (Left / Center)

Reimplementations of built-in Python string methods without using the original function.

| # | File | Description |
|---|------|-------------|
| 1 | `7/lstrip.py` | Remove space characters at the beginning of a string (without `lstrip()`) |
| 2 | `7/removeprefix.py` | Remove matching characters at the beginning of a string (without `removeprefix()`) |
| 3 | `7/lower.py` | Convert all characters to lower case (without `lower()`) |
| 4 | `7/isupper.py` | Check if all characters are upper case (without `isupper()`) |
| 5 | `7/endswith.py` | Check if a string ends with a given substring (without `endswith()`) |
| 6 | `7/ljust.py` | Pad the end of a string with spaces to a given width (without `ljust()`) |
| 7 | `7/center.py` | Center a string by padding with spaces on both sides (without `center()`) |
| 8 | `7/swapcase.py` | Reverse the casing of each character (without `swapcase()`) |
| 9 | `7/capitalize.py` | Capitalize the first letter, lowercase the rest (without `capitalize()`) |
| 10 | `7/title.py` | Capitalize the first letter of each word (without `title()`) |
